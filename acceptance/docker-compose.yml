# 一次性 NetBox 验收栈：postgres + redis + netbox。
# 仅用于本地验收，跑完即由 acceptance/run.sh 通过 `down -v` 销毁，可重复执行。
# NetBox 固定到 4.2（netbox-docker 3.2.1），使用经典 API Token 机制，
# 与 nbx-guard 的 `Authorization: Token <token>` 兼容。

services:
  postgres:
    image: docker.io/postgres:16-alpine
    environment:
      POSTGRES_DB: netbox
      POSTGRES_USER: netbox
      POSTGRES_PASSWORD: netbox
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -q -U netbox -d netbox"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    tmpfs:
      - /var/lib/postgresql/data

  redis:
    image: docker.io/redis:7-alpine
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep -q PONG"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 3s

  netbox:
    image: docker.io/netboxcommunity/netbox:v4.2-3.2.1
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      # 数据库
      DB_HOST: postgres
      DB_NAME: netbox
      DB_USER: netbox
      DB_PASSWORD: netbox
      # Redis：tasks 用 db0，cache 用 db1，同一实例
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DATABASE: "0"
      REDIS_CACHE_HOST: redis
      REDIS_CACHE_PORT: "6379"
      REDIS_CACHE_DATABASE: "1"
      # NetBox 自身
      SECRET_KEY: "nbx-guard-acceptance-secret-key-please-do-not-reuse-0123456789"
      ALLOWED_HOSTS: "*"
      # 自动创建超级用户与确定性的经典 API Token（验收用，固定值）
      SKIP_SUPERUSER: "false"
      SUPERUSER_NAME: admin
      SUPERUSER_EMAIL: admin@example.com
      SUPERUSER_PASSWORD: admin
      SUPERUSER_API_TOKEN: "0123456789abcdef0123456789abcdef01234567"
      # 验收无需后台任务，加快冷启动
      HOUSEKEEPING_INTERVAL: "0"
    ports:
      - "${NBX_NETBOX_PORT:-8000}:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/login/ >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 120s
